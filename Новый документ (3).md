#### **8\. /core/acp\_core\_v1\_0.py**

**Назначение:** Замороженный детерминированный модуль. **Статус:** FROZEN / CANONICAL **Зависимости:** `typing`, `enum`, `dataclasses`

Python  
"""  
ACP-CORE MICRO-AGENT v1.0  
Minimal Deterministic Engineering Reflex  
Status: FROZEN / CANONICAL  
"""  
from dataclasses import dataclass  
from typing import Dict, Any, Optional  
from enum import IntEnum

class State(IntEnum):  
    STABLE \= 0  
    WARNING \= 1  
    CRITICAL \= 2

@dataclass  
class ACPConfig:  
    warning\_threshold: float \= 0.33  
    critical\_threshold: float \= 0.69  
    weight\_wear: float \= 1.0  
    weight\_instability: float \= 0.40  
    weight\_hidden: float \= 0.76  
    weight\_burden: float \= 0.68  
    weight\_oscillation: float \= 0.22  
    stability\_limit: float \= 0.90  
    wear\_effort\_coef: float \= 0.68

class ACPCore:  
    def \_\_init\_\_(self, config: Optional\[ACPConfig\] \= None):  
        self.config \= config or ACPConfig()  
        self.\_prev\_effort \= 0.0  
        self.\_prev\_velocity \= 0.0  
        self.\_prev\_wear \= 0.0  
      
    def evaluate(self, effort: float, velocity: float, wear: float, stability: float) \-\> Dict\[str, Any\]:  
        effort, velocity, wear, stability \= \[max(0.0, min(1.0, x)) for x in \[effort, velocity, wear, stability\]\]  
          
        de \= effort \- self.\_prev\_effort  
        dv \= velocity \- self.\_prev\_velocity  
        dw \= wear \- self.\_prev\_wear  
          
        instability \= max(0.0, de \- dv)  
        hidden \= effort \* max(0.0, self.config.stability\_limit \- stability)  
        burden \= wear \* effort \* self.config.wear\_effort\_coef  
        oscillation \= dv \* dv  
          
        risk \= (max(0.0, dw) \* self.config.weight\_wear \+   
                instability \* self.config.weight\_instability \+   
                hidden \* self.config.weight\_hidden \+   
                burden \* self.config.weight\_burden \+   
                oscillation \* self.config.weight\_oscillation)  
          
        if risk \> self.config.critical\_threshold:  
            state \= State.CRITICAL  
        elif risk \> self.config.warning\_threshold:  
            state \= State.WARNING  
        else:  
            state \= State.STABLE  
              
        self.\_prev\_effort \= effort  
        self.\_prev\_velocity \= velocity  
        self.\_prev\_wear \= wear  
          
        return {  
            'state': state,   
            'state\_name': state.name,   
            'risk': round(risk, 4\)  
        }

