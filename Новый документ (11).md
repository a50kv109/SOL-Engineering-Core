#### **21\. /tests/test\_false\_negative.py**

**Назначение:** Проверка устойчивости к ложным отрицаниям (пропуск аварии). **Статус:** STABLE **Зависимости:** `acp_core_v1_0`

Python  
import unittest  
from core.acp\_core\_v1\_0 import ACPCore, State

class TestFalseNegative(unittest.TestCase):  
    def test\_hidden\_degradation(self):  
        acp \= ACPCore()  
        \# Внешне нагрузка и скорость малы, но износ максимален, а стабильность утеряна  
        report \= acp.evaluate(effort=0.1, velocity=0.1, wear=0.95, stability=0.1)  
        \# Система должна увидеть деградацию (минимум WARNING)  
        self.assertIn(report\['state'\], \[State.WARNING, State.CRITICAL\])

if \_\_name\_\_ \== '\_\_main\_\_':  
    unittest.main()  
